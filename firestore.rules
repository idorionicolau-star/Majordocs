
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regra temporária e permissiva para desenvolvimento.
    // Permite que qualquer pessoa leia e escreva em qualquer coleção.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

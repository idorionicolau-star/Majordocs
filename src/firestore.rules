
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Em modo de utilizador único, permitir acesso aberto.
    // A segurança é gerida pela posse física do dispositivo.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
